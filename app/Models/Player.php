<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Model;

class Player extends Model {

	/**
	 * The model doesn't use timestamp such as `created_at` and `updated_at`.
	 *
	 * @var bool
	 */
	public $timestamps = false;

}
